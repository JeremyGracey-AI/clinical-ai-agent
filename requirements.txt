# Hugging Face Spaces runtime deps (full stack).
# Core runs offline with just the first three.
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
gradio>=4.0
# Optional — enable real RAG / LLM / graph:
# chromadb>=0.5            # USE_CHROMA=true
# sentence-transformers>=3.0  # EMBED_PROVIDER=sentence-transformers
# langgraph>=0.2
# langchain>=0.2
# openai>=1.30             # LLM_PROVIDER=openai
# anthropic>=0.40          # LLM_PROVIDER=anthropic
